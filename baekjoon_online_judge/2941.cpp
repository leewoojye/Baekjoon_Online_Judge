#include <iostream>
#include <string>
using namespace std;
#define fastio ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

int main() {
  fastio;
  string s;
  cin >> s;
  int index=0, count=0;
  while(s[index]!='\0') [
    if(s[index]=='=' || s[index]=='-' || s[index])
  ]
  return 0;
}